/**
 * Widgets Barrel Export
 */

export {
    WidgetContainer,
    StatsCard,
    RecentActivityWidget,
    QuickActionsWidget,
    SOSMonitorWidget,
    TaskListWidget,
    WeatherAlertWidget,
} from './DashboardWidgets';

export type { WidgetConfig, WidgetType } from './DashboardWidgets';
